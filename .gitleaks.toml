[extend]
useDefault = true

[[allowlists]]
description = "False positives in minified vendor assets (Playwright trace viewer)"
paths = [
    '''\d+/playwright-trace-viewer/''',
]

[[allowlists]]
description = "False positives in npm lockfiles (integrity hashes match secret patterns)"
paths = [
    '''package-lock\.json$''',
]
